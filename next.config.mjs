/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    trailingSlash: true,
    basePath: '/kwarcabwonogiri-web',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
